eureka:
    instance:
        hostname: localhost

    client:
        serviceurl:
            defaultZone: http://localhost:8761/eureka/
server:
    port: 8762

spring:
    application:
        name: eureka-client
